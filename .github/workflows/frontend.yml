# frontend.yml — CI workflow for the Next.js frontend
# Placeholder: full implementation will be added in the deployment phase.
#
# Intended steps when implemented:
#   1. Checkout code
#   2. Set up Node.js 20
#   3. Cache npm dependencies
#   4. Run: npm ci
#   5. Run: npm run lint
#   6. Run: npm run build

name: Frontend CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'frontend/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'frontend/**'

jobs:
  placeholder:
    name: Frontend CI (Placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder step
        run: echo "Frontend CI workflow — implementation pending. See docs/21-deployment.md"
